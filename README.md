# Cryptography-Library-cpp
C++ library for performing cryptographic computation on large numbers

I've used CodeBlocks(random IDE) to build the code. In case you're not using CodeBlocks, ignore the .cbp file.
